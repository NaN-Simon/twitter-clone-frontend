const values = {
  xs: 0,
  sm: 768,
  md: 1024,
  lg: 1300,
  xl: 1920,
 };

 const devices = {
  xs: `${values.xs}px`,
  sm: `${values.sm}px`,
  md: `${values.md}px`,
  lg: `${values.lg}px`,
  xl: `${values.xl}px`,
 };

 const breakpoints = {
  values,
  devices,
 };

 export default breakpoints;
